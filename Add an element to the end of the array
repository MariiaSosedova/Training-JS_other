Write a function pushElement that takes the arr array and value as arguments adds value to the end of the array and returns the resulting array.

Example:

pushElement ([1, 2, 3], "a")  return [1, 2, 3, "a"];
pushElement ([0, 0, 0], 5)  return [0, 0, 0, 5];
pushElement ([], 1) return [1].

Solution:

function pushElement(arr, value){
  arr.push(value);
  return arr;
}
