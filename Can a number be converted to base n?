Write a function named convertNumber that takes an integer positive decimal number num and a number n (base) as arguments 
and returns a base-n number or undefined if it cannot be converted to that base.

to Examples:

convertNumber (9, 2) should return "1001";
convertNumber (135, 40) function should return undefined


Solution:

function convertNumber(num, n){
  return n >= 2 && n <= 36 ? num.toString(n) : undefined;
}
