Write a function createOddArray that takes a non-negative integer n as an argument and returns an array containing n odd numbers.

Example:

createOddArray (5) should return [1, 3, 5, 7, 9];
createOddArray (2) should return [1, 3];
createOddArray (1) should return [1];
createOddArray (0) should return []

Solution:

function createOddArray(n){
  let arr = [];
  for(let i = 0; i < n * 2; i++){
    if (i % 2 !== 0) arr.push(i);
  }
  return arr;
}
