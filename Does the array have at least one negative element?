Write a function isNegativeInArray that takes the arr array as an argument and returns true if the array contains at least one negative element and false otherwise.

Example:

isNegativeInArray ([20, -26, -12]) freturn true;
isNegativeInArray ([2, 9, -10]) return true;
isNegativeInArray ([1, 2, 3, 4]) return false;
isNegativeInArray ([1, 0]) return false;
isNegativeInArray ([]) return false.

Solution:

function isNegativeInArray(arr){
  for(let el of arr) {
    if (el < 0) return true;
  }
  return false;
}
