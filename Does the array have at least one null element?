Write the isZeroInArray function that takes the arr array as an argument and returns true if the array contains at least one zero element, and false otherwise.

Example:

isZeroInArray ([20, -26, 0, -12]) return true;
isZeroInArray ([2, 9, 0, 0]) return true;
isZeroInArray ([1, 2, 3, 4]) return false;
isZeroInArray ([1, -1]) return false;
isZeroInArray ([])  return false.

Solution:

function isZeroInArray(arr){
  for(let el of arr) {
    if (el === 0) return true;
  }
  return false;
}
