Write a min function that takes an arr array as an argument and returns the minimum element of that array. 
If the array is empty, the function should return [].

Examples:

min ([7, 23, 34, 9, 4, 45]) should return 4.
min ([]) should return [].

Solution:

function min(arr){
  return arr.length === 0 ? [] : Math.min(...arr);
}
