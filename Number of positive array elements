Write a function countPositive that takes an array arr as an argument and returns the number of positive elements in the array.

Example:

countPositive ([10, 2, -3, 8]) should return 3;
countPositive ([0, -1, -2]) should return 0;
countPositive ([]) should return 0

Solution:

function countPositive(arr){
  let count = 0;
  for(let el of arr) {
    if (el > 0) count++;
  }
  return count;
}
