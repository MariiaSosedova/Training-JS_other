Write a function sumOfEvenPositive that takes an arr array as an argument and returns the sum of the even positive elements of the array.
Use a loop in your solution. Array methods are not allowed.

Example:
sumOfEvenPositive ([2, 2, 3, -2, - 4]) return 4;
sumOfEvenPositive ([15, -13]) return 0;
sumOfEvenPositive ([]) return 0.

Solution:

function sumOfEvenPositive(arr){
  let sum = 0;
  for(let el of arr) {
    if (el > 0 && el % 2 === 0) sum += el;
  }
  return sum;
}
